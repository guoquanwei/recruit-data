<section class="empty-state card border-0 shadow-sm">
  <div class="card-body p-5 text-center">
    <div class="empty-state-icon mb-3">HR</div>
    <h1 class="h3 mb-3"><%= heading %></h1>
    <p class="text-secondary mb-4"><%= description %></p>
    <p class="small text-muted mb-0">当前版本仅搭建平台入口和页面骨架，业务数据将在后续需求中接入。</p>
  </div>
</section>
